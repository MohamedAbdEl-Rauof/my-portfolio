# Public origin used for canonical URLs, the sitemap, OG tags and JSON-LD.
# A production build that resolves a non-https origin fails on purpose.
NEXT_PUBLIC_SITE_URL=https://developer-moraouf.vercel.app
